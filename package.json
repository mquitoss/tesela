{
  "name": "tesela-map",
  "version": "0.2.0",
  "private": true,
  "description": "Motor y shell zero-build para crear aplicaciones cartográficas configurables.",
  "type": "commonjs",
  "scripts": {
    "release": "node scripts/release.js",
    "test": "vitest run",
    "test:watch": "vitest",
    "version:check": "node -e \"const r=require('./scripts/release');const p=require('./package');r.assertVersionSynchronized(process.cwd(),p.version)\""
  },
  "devDependencies": {
    "vitest": "^3.2.4"
  }
}
