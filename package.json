{
  "name": "self-service-map",
  "version": "0.1.0",
  "private": true,
  "description": "Framework base zero-build para crear mapas que muestran datos (coropleta + scoring), personalizable por config + adaptadores.",
  "type": "commonjs",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vitest": "^2.1.9"
  }
}
